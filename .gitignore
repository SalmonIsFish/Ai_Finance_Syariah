backend/.env
.venv/
__pycache__/
*.pyc
backend/*.db
backend/market_data_cache/
backend/sec_edgar_cache/
tmp/
.ruff_cache/

# Local SQLite backups from backend/backup_database.py's default location.
# On the VPS this is redirected via PAPER_DB_BACKUP_DIR to a path outside the
# checkout entirely -- see docs/deployment/VPS_RUNBOOK.md's Backups section.
/backups/

# LLM council consult tooling (contains real API keys in its own .env)
llm-council-skill/

# Git worktrees used during development
.worktrees/

# Local state
backend/*.sqlite
backend/*.sqlite3
.pytest_cache/
*.egg-info/

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
*.bak
*.orig
*.rej

# Reviewed previews staged by check_paper_order.py
backend/staged_preview_*.json

# Local Playwright MCP verification traces
.playwright-mcp/

# Reference-only design material, not part of the shipped dashboard
TradingDesignIdea/

# Internal planning notes -- not meant for a public/judge-facing repo.
# Kept locally, untracked; see git history before this commit if ever needed.
NEXT_STEPS.md
PROJECT_VISION.md

Claude outputs/
scratch/
test_output.txt
CararunCode.txt
.claude/settings.local.json
